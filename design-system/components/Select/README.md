# Select

Select — native select styled to match Input.

**Consumer provides:** a `ds-label`, the options, and a placeholder option when nothing is chosen. For searchable or multi-select lists build on `Menu` instead.

- The chevron is drawn by the stylesheet (an inline SVG background); don't add another.
- Keep option labels short; put the unit in the label ("Interval (ms)") not in every option.
